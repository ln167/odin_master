// Lesson 01b "break it" #1: a second file in mymath/ declares a DIFFERENT
// package name. One folder is one package, so the build must refuse this.
package mymath_constants

PI :: 3.14159265358979
VERSION :: "0.1.0"
