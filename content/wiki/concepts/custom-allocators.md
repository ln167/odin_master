# Custom Allocators

Odin's `Allocator` interface is built into the language's `base` library, allowing custom memory allocation strategies. This enables uniform handling of allocators across both user code and core libraries, supporting advanced allocation patterns like arena-based memory management and object pooling.

<!-- source: C:/Users/user1/dev/odin_master/content/sources/tier2-curated/karl-zylinski -->
