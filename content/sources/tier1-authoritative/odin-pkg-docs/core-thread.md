package thread - pkg.odin-lang.org 






[![Odin](https://odin-lang.org/logo.svg)](https://odin-lang.org/)


* [Home](https://odin-lang.org/)
* [Docs](https://odin-lang.org/docs)
* [Packages](/)
* [News](https://odin-lang.org/news)
* [Showcase](https://odin-lang.org/showcase)
* [Forum](https://forum.odin-lang.org)
* [Community](https://odin-lang.org/community)
* [GitHub](https://github.com/odin-lang/Odin)
* Appearance:



Current Package: *[thread](/core/thread)*

  

#### [core Library](/core)

* [bufio](/core/bufio)
* [bytes](/core/bytes)
* [c](/core/c)
  + [libc](/core/c/libc)
* [compress](/core/compress)
  + [gzip](/core/compress/gzip)
  + [shoco](/core/compress/shoco)
  + [zlib](/core/compress/zlib)
* container
  + [avl](/core/container/avl)
  + [bit\_array](/core/container/bit_array)
  + [handle\_map](/core/container/handle_map)
  + [intrusive/list](/core/container/intrusive/list)
  + [lru](/core/container/lru)
  + [pool](/core/container/pool)
  + [priority\_queue](/core/container/priority_queue)
  + [queue](/core/container/queue)
  + [rbtree](/core/container/rbtree)
  + [small\_array](/core/container/small_array)
  + [topological\_sort](/core/container/topological_sort)
  + [xar](/core/container/xar)
* [crypto](/core/crypto)
  + [aead](/core/crypto/aead)
  + [aegis](/core/crypto/aegis)
  + [aes](/core/crypto/aes)
  + [argon2id](/core/crypto/argon2id)
  + [blake2b](/core/crypto/blake2b)
  + [blake2s](/core/crypto/blake2s)
  + [chacha20](/core/crypto/chacha20)
  + [chacha20poly1305](/core/crypto/chacha20poly1305)
  + [deoxysii](/core/crypto/deoxysii)
  + [ecdh](/core/crypto/ecdh)
  + [ecdsa](/core/crypto/ecdsa)
  + [ed25519](/core/crypto/ed25519)
  + [hash](/core/crypto/hash)
  + [hkdf](/core/crypto/hkdf)
  + [hmac](/core/crypto/hmac)
  + [kmac](/core/crypto/kmac)
  + [legacy/keccak](/core/crypto/legacy/keccak)
  + [legacy/md5](/core/crypto/legacy/md5)
  + [legacy/sha1](/core/crypto/legacy/sha1)
  + [pbkdf2](/core/crypto/pbkdf2)
  + [poly1305](/core/crypto/poly1305)
  + [ristretto255](/core/crypto/ristretto255)
  + [sha2](/core/crypto/sha2)
  + [sha3](/core/crypto/sha3)
  + [shake](/core/crypto/shake)
  + [sm3](/core/crypto/sm3)
  + [tuplehash](/core/crypto/tuplehash)
  + [x25519](/core/crypto/x25519)
  + [x448](/core/crypto/x448)
* debug
  + [pe](/core/debug/pe)
  + [trace](/core/debug/trace)
* [dynlib](/core/dynlib)
* encoding
  + [base32](/core/encoding/base32)
  + [base64](/core/encoding/base64)
  + [cbor](/core/encoding/cbor)
  + [csv](/core/encoding/csv)
  + [endian](/core/encoding/endian)
  + [entity](/core/encoding/entity)
  + [hex](/core/encoding/hex)
  + [hxa](/core/encoding/hxa)
  + [ini](/core/encoding/ini)
  + [json](/core/encoding/json)
  + [uuid](/core/encoding/uuid)
  + [uuid/legacy](/core/encoding/uuid/legacy)
  + [varint](/core/encoding/varint)
  + [xml](/core/encoding/xml)
* [flags](/core/flags)
* [fmt](/core/fmt)
* [hash](/core/hash)
  + [xxhash](/core/hash/xxhash)
* [image](/core/image)
  + [bmp](/core/image/bmp)
  + [jpeg](/core/image/jpeg)
  + [netpbm](/core/image/netpbm)
  + [png](/core/image/png)
  + [qoi](/core/image/qoi)
  + [tga](/core/image/tga)
* [io](/core/io)
* [log](/core/log)
* [math](/core/math)
  + [big](/core/math/big)
  + [bits](/core/math/bits)
  + [cmplx](/core/math/cmplx)
  + [ease](/core/math/ease)
  + [fixed](/core/math/fixed)
  + [linalg](/core/math/linalg)
  + [linalg/glsl](/core/math/linalg/glsl)
  + [linalg/hlsl](/core/math/linalg/hlsl)
  + [noise](/core/math/noise)
  + [rand](/core/math/rand)
* [mem](/core/mem)
  + [tlsf](/core/mem/tlsf)
  + [virtual](/core/mem/virtual)
* [nbio](/core/nbio)
* [net](/core/net)
* odin
  + [ast](/core/odin/ast)
  + [doc-format](/core/odin/doc-format)
  + [parser](/core/odin/parser)
  + [tokenizer](/core/odin/tokenizer)
* [os](/core/os)
  + [old](/core/os/old)
* path
  + [filepath](/core/path/filepath)
  + [slashpath](/core/path/slashpath)
* prof
  + [spall](/core/prof/spall)
* [reflect](/core/reflect)
* [relative](/core/relative)
* [simd](/core/simd)
  + [x86](/core/simd/x86)
* [slice](/core/slice)
  + [heap](/core/slice/heap)
* [sort](/core/sort)
* [strconv](/core/strconv)
  + [decimal](/core/strconv/decimal)
* [strings](/core/strings)
* [sync](/core/sync)
  + [chan](/core/sync/chan)
* sys
  + [darwin](/core/sys/darwin)
  + [darwin/CoreFoundation](/core/sys/darwin/CoreFoundation)
  + [darwin/CoreGraphics](/core/sys/darwin/CoreGraphics)
  + [darwin/Foundation](/core/sys/darwin/Foundation)
  + [darwin/Security](/core/sys/darwin/Security)
  + [freebsd](/core/sys/freebsd)
  + [haiku](/core/sys/haiku)
  + [info](/core/sys/info)
  + [kqueue](/core/sys/kqueue)
  + [linux](/core/sys/linux)
  + [linux/uring](/core/sys/linux/uring)
  + [orca](/core/sys/orca)
  + [posix](/core/sys/posix)
  + [unix](/core/sys/unix)
  + [valgrind](/core/sys/valgrind)
  + [wasm/js](/core/sys/wasm/js)
  + [wasm/wasi](/core/sys/wasm/wasi)
  + [windows](/core/sys/windows)
* [terminal](/core/terminal)
  + [ansi](/core/terminal/ansi)
* [testing](/core/testing)
* text
  + [edit](/core/text/edit)
  + [i18n](/core/text/i18n)
  + [match](/core/text/match)
  + [regex](/core/text/regex)
  + [regex/common](/core/text/regex/common)
  + [regex/compiler](/core/text/regex/compiler)
  + [regex/optimizer](/core/text/regex/optimizer)
  + [regex/parser](/core/text/regex/parser)
  + [regex/tokenizer](/core/text/regex/tokenizer)
  + [regex/virtual\_machine](/core/text/regex/virtual_machine)
  + [scanner](/core/text/scanner)
  + [table](/core/text/table)
* [thread](/core/thread)
* [time](/core/time)
* [unicode](/core/unicode)
  + [utf16](/core/unicode/utf16)
  + [utf8](/core/unicode/utf8)
  + [utf8/utf8string](/core/unicode/utf8/utf8string)

1. [core](/core)
2. [thread](/core/thread)

# package core:thread [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread)

⌘K

Ctrl+K

or

/

Filter Results



## Overview

Multi-threading operations to spawn threads and thread pools.

## Index

Types (11)

* [Pool](#Pool)
* [Pool\_Thread\_Data](#Pool_Thread_Data)
* [Task](#Task)
* [Task\_Proc](#Task_Proc)
* [Thread](#Thread)
* [Thread\_Fini\_Proc](#Thread_Fini_Proc)
* [Thread\_Init\_Proc](#Thread_Init_Proc)
* [Thread\_Os\_Specific](#Thread_Os_Specific)
* [Thread\_Priority](#Thread_Priority)
* [Thread\_Proc](#Thread_Proc)
* [Thread\_State](#Thread_State)

Constants (2)

* [IS\_SUPPORTED](#IS_SUPPORTED)
* [MAX\_USER\_ARGUMENTS](#MAX_USER_ARGUMENTS)

Variables (0)

This section is empty.

Procedures (37)

* [create](#create)
* [create\_and\_start](#create_and_start)
* [create\_and\_start\_with\_data](#create_and_start_with_data)
* [create\_and\_start\_with\_poly\_data](#create_and_start_with_poly_data)
* [create\_and\_start\_with\_poly\_data2](#create_and_start_with_poly_data2)
* [create\_and\_start\_with\_poly\_data3](#create_and_start_with_poly_data3)
* [create\_and\_start\_with\_poly\_data4](#create_and_start_with_poly_data4)
* [destroy](#destroy)
* [is\_done](#is_done)
* [join](#join)
* [join\_multiple](#join_multiple)
* [pool\_add\_task](#pool_add_task)
* [pool\_destroy](#pool_destroy)
* [pool\_do\_work](#pool_do_work)
* [pool\_finish](#pool_finish)
* [pool\_init](#pool_init)
* [pool\_is\_empty](#pool_is_empty)
* [pool\_join](#pool_join)
* [pool\_num\_done](#pool_num_done)
* [pool\_num\_in\_processing](#pool_num_in_processing)
* [pool\_num\_outstanding](#pool_num_outstanding)
* [pool\_num\_waiting](#pool_num_waiting)
* [pool\_pop\_done](#pool_pop_done)
* [pool\_pop\_waiting](#pool_pop_waiting)
* [pool\_shutdown](#pool_shutdown)
* [pool\_start](#pool_start)
* [pool\_stop\_all\_tasks](#pool_stop_all_tasks)
* [pool\_stop\_task](#pool_stop_task)
* [run](#run)
* [run\_with\_data](#run_with_data)
* [run\_with\_poly\_data](#run_with_poly_data)
* [run\_with\_poly\_data2](#run_with_poly_data2)
* [run\_with\_poly\_data3](#run_with_poly_data3)
* [run\_with\_poly\_data4](#run_with_poly_data4)
* [start](#start)
* [terminate](#terminate)
* [yield](#yield)

Procedure Groups (0)

This section is empty.

## Types

### [Pool ¶](#Pool) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L30)

```
Pool :: struct {
	allocator:         runtime.Allocator,
	mutex:             sync.Mutex,
	sem_available:     sync.Sema,
	// the following values are atomic
	num_waiting:       int,
	num_in_processing: int,
	num_outstanding:   int,
	// num_waiting + num_in_processing
	num_done:          int,
	// called once per thread at startup
	thread_init_proc:  Thread_Init_Proc,
	thread_init_data:  rawptr,
	// called once per thread at shutdown
	thread_fini_proc:  Thread_Fini_Proc,
	thread_fini_data:  rawptr,
	is_running:        bool,
	threads:           []^Thread,
	tasks:             container_queue.Queue($T=Task),
	tasks_done:        [dynamic]Task,
}
```

 

Do not access the pool's members directly while the pool threads are running,
since they use different kinds of locking and mutual exclusion devices.
Careless access can and will lead to nasty bugs. Once initialized, the
pool's memory address is not allowed to change until it is destroyed.




##### Related Procedures With Parameters

* [pool\_add\_task](/core/thread/#pool_add_task)
* [pool\_destroy](/core/thread/#pool_destroy)
* [pool\_do\_work](/core/thread/#pool_do_work)
* [pool\_finish](/core/thread/#pool_finish)
* [pool\_init](/core/thread/#pool_init)
* [pool\_is\_empty](/core/thread/#pool_is_empty)
* [pool\_join](/core/thread/#pool_join)
* [pool\_num\_done](/core/thread/#pool_num_done)
* [pool\_num\_in\_processing](/core/thread/#pool_num_in_processing)
* [pool\_num\_outstanding](/core/thread/#pool_num_outstanding)
* [pool\_num\_waiting](/core/thread/#pool_num_waiting)
* [pool\_pop\_done](/core/thread/#pool_pop_done)
* [pool\_pop\_waiting](/core/thread/#pool_pop_waiting)
* [pool\_shutdown](/core/thread/#pool_shutdown)
* [pool\_start](/core/thread/#pool_start)
* [pool\_stop\_all\_tasks](/core/thread/#pool_stop_all_tasks)
* [pool\_stop\_task](/core/thread/#pool_stop_task)

### [Pool\_Thread\_Data ¶](#Pool_Thread_Data) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L58)

```
Pool_Thread_Data :: struct {
	pool: ^Pool,
	task: Task,
}
```

### [Task ¶](#Task) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L16)

```
Task :: struct {
	procedure:  Task_Proc,
	data:       rawptr,
	user_index: int,
	allocator:  runtime.Allocator,
}
```

##### Related Procedures With Parameters

* [pool\_do\_work](/core/thread/#pool_do_work)



##### Related Procedures With Returns

* [pool\_pop\_done](/core/thread/#pool_pop_done)
* [pool\_pop\_waiting](/core/thread/#pool_pop_waiting)

### [Task\_Proc ¶](#Task_Proc) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L14)

```
Task_Proc :: proc(task: Task)
```

##### Related Procedures With Parameters

* [pool\_add\_task](/core/thread/#pool_add_task)

### [Thread ¶](#Thread) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L43)

```
Thread :: struct {
	using specific:     Thread_Os_Specific,
	flags:              bit_set[Thread_State; u8],
	// Thread ID. Depending on the platform, may start out as 0 (zero) until the thread
	// has had a chance to run.
	id:                 int,
	// The thread procedure.
	procedure:          Thread_Proc,
	// User-supplied pointer, that will be available to the thread once it is
	// started. Should be set after the thread has been created, but before
	// it is started.
	data:               rawptr,
	// User-supplied integer, that will be available to the thread once it is
	// started. Should be set after the thread has been created, but before
	// it is started.
	user_index:         int,
	// User-supplied array of arguments, that will be available to the thread,
	// once it is started. Should be set after the thread has been created,
	// but before it is started.
	user_args:          [8]rawptr,
	// The thread context.
	// This field can be assigned to directly, after the thread has been
	// created, but __before__ the thread has been started. This field must
	// not be changed after the thread has started.
	// 
	// **Note**: If this field is **not** set, the temp allocator will be managed
	// automatically. If it is set, the allocators must be handled manually.
	// 
	// **IMPORTANT**:
	// By default, the thread proc will get the same context as `main()` gets.
	// In this situation, the thread will get a new temporary allocator which
	// will be cleaned up when the thread dies. ***This does NOT happen when
	// `init_context` field is initialized***.
	// 
	// If `init_context` is initialized, and `temp_allocator` field is set to
	// the default temp allocator, then `runtime.default_temp_allocator_destroy()`
	// procedure needs to be called from the thread procedure, in order to prevent
	// any memory leaks.
	init_context:       runtime.Maybe($T=Context),
	// The allocator used to allocate data for the thread.
	creation_allocator: runtime.Allocator,
}
```

 

Type representing a thread handle and the associated with that thread data.




##### Related Procedures With Parameters

* [destroy](/core/thread/#destroy)
* [is\_done](/core/thread/#is_done)
* [join](/core/thread/#join)
* [start](/core/thread/#start)
* [terminate](/core/thread/#terminate)



##### Related Procedures With Returns

* [create](/core/thread/#create)
* [create\_and\_start](/core/thread/#create_and_start)
* [create\_and\_start\_with\_data](/core/thread/#create_and_start_with_data)
* [create\_and\_start\_with\_poly\_data](/core/thread/#create_and_start_with_poly_data)
* [create\_and\_start\_with\_poly\_data2](/core/thread/#create_and_start_with_poly_data2)
* [create\_and\_start\_with\_poly\_data3](/core/thread/#create_and_start_with_poly_data3)
* [create\_and\_start\_with\_poly\_data4](/core/thread/#create_and_start_with_poly_data4)

### [Thread\_Fini\_Proc ¶](#Thread_Fini_Proc) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L24)

```
Thread_Fini_Proc :: proc(thread: ^Thread, user_data: rawptr)
```

### [Thread\_Init\_Proc ¶](#Thread_Init_Proc) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L23)

```
Thread_Init_Proc :: proc(thread: ^Thread, user_data: rawptr)
```

##### Related Procedures With Parameters

* [pool\_init](/core/thread/#pool_init)

### [Thread\_Os\_Specific ¶](#Thread_Os_Specific) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_other.odin#L8)

```
Thread_Os_Specific :: struct {}
```

### [Thread\_Priority ¶](#Thread_Priority) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L93)

```
Thread_Priority :: enum int {
	Normal, 
	Low, 
	High, 
}
```

 

Type representing priority of a thread.




##### Related Procedures With Parameters

* [create](/core/thread/#create)
* [create\_and\_start](/core/thread/#create_and_start)
* [create\_and\_start\_with\_data](/core/thread/#create_and_start_with_data)
* [create\_and\_start\_with\_poly\_data](/core/thread/#create_and_start_with_poly_data)
* [create\_and\_start\_with\_poly\_data2](/core/thread/#create_and_start_with_poly_data2)
* [create\_and\_start\_with\_poly\_data3](/core/thread/#create_and_start_with_poly_data3)
* [create\_and\_start\_with\_poly\_data4](/core/thread/#create_and_start_with_poly_data4)
* [run](/core/thread/#run)
* [run\_with\_data](/core/thread/#run_with_data)
* [run\_with\_poly\_data](/core/thread/#run_with_poly_data)
* [run\_with\_poly\_data2](/core/thread/#run_with_poly_data2)
* [run\_with\_poly\_data3](/core/thread/#run_with_poly_data3)
* [run\_with\_poly\_data4](/core/thread/#run_with_poly_data4)

### [Thread\_Proc ¶](#Thread_Proc) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L23)

```
Thread_Proc :: proc(^Thread)
```

 

Type for a procedure that will be run in a thread, after that thread has been
started.




##### Related Procedures With Parameters

* [create](/core/thread/#create)

### [Thread\_State ¶](#Thread_State) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L33)

```
Thread_State :: enum u8 {
	Started, 
	Joined, 
	Done, 
	Self_Cleanup, 
}
```

 

Type representing the state/flags of the thread.

## Constants

### [IS\_SUPPORTED ¶](#IS_SUPPORTED) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L17)

```
IS_SUPPORTED :: _IS_SUPPORTED
```

 

Value, specifying whether `core:thread` functionality is available on the
current platform.

### [MAX\_USER\_ARGUMENTS ¶](#MAX_USER_ARGUMENTS) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L28)

```
MAX_USER_ARGUMENTS :: 8
```

 

Maximum number of user arguments for polymorphic thread procedures.

## Variables

This section is empty.

## Procedures

### [create ¶](#create) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L109)

```
create :: proc(procedure: Thread_Proc, priority: Thread_Priority = Thread_Priority.Normal) -> ^Thread {…}
```

 

Create a thread in a suspended state with the given priority.

This procedure creates a thread that will be set to run the procedure
specified by `procedure` parameter with a specified priority. The returned
thread will be in a suspended state, until `start()` procedure is called.

To start the thread, call `start()`. Also the `create_and_start()`
procedure can be called to create and start the thread immediately.

### [create\_and\_start ¶](#create_and_start) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L274)

```
create_and_start :: proc(fn: proc(), init_context: runtime.Maybe($T=Context) = nil, priority: Thread_Priority = Thread_Priority.Normal, self_cleanup: bool = false) -> (t: ^Thread) {…}
```

 

Run a procedure on a different thread.

This procedure runs the given procedure on another thread. The context
specified by `init_context` will be used as the context in which `fn` is going
to execute. The thread will have priority specified by the `priority` parameter.

If `self_cleanup` is specified, after the thread finishes the execution of the
`fn` procedure, the resources associated with the thread are going to be
automatically freed.

**Do not** dereference the `^Thread` pointer, if this flag is specified.
That includes calling `join`, which needs to dereference ^Thread`.

**IMPORTANT**: If `init_context` is specified and the default temporary allocator
is used, the thread procedure needs to call `runtime.default_temp_allocator_destroy()`
in order to free the resources associated with the temporary allocations.

### [create\_and\_start\_with\_data ¶](#create_and_start_with_data) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L309)

```
create_and_start_with_data :: proc(data: rawptr, fn: proc(data: rawptr), init_context: runtime.Maybe($T=Context) = nil, priority: Thread_Priority = Thread_Priority.Normal, self_cleanup: bool = false) -> (t: ^Thread) {…}
```

 

Run a procedure with one pointer parameter on a different thread.

This procedure runs the given procedure on another thread. The context
specified by `init_context` will be used as the context in which `fn` is going
to execute. The thread will have priority specified by the `priority` parameter.

If `self_cleanup` is specified, after the thread finishes the execution of the
`fn` procedure, the resources associated with the thread are going to be
automatically freed.

**Do not** dereference the `^Thread` pointer, if this flag is specified.
That includes calling `join`, which needs to dereference ^Thread`.

**IMPORTANT**: If `init_context` is specified and the default temporary allocator
is used, the thread procedure needs to call `runtime.default_temp_allocator_destroy()`
in order to free the resources associated with the temporary allocations.

### [create\_and\_start\_with\_poly\_data ¶](#create_and_start_with_poly_data) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L348)

```
create_and_start_with_poly_data :: proc(data: $T, fn: proc(data: $T), init_context: runtime.Maybe($T=Context) = nil, priority: Thread_Priority = Thread_Priority.Normal, self_cleanup: bool = false) -> (t: ^Thread) {…}
```

 

Run a procedure with one polymorphic parameter on a different thread.

This procedure runs the given procedure on another thread. The context
specified by `init_context` will be used as the context in which `fn` is going
to execute. The thread will have priority specified by the `priority` parameter.

If `self_cleanup` is specified, after the thread finishes the execution of the
`fn` procedure, the resources associated with the thread are going to be
automatically freed.

**Do not** dereference the `^Thread` pointer, if this flag is specified.
That includes calling `join`, which needs to dereference ^Thread`.

**IMPORTANT**: If `init_context` is specified and the default temporary allocator
is used, the thread procedure needs to call `runtime.default_temp_allocator_destroy()`
in order to free the resources associated with the temporary allocations.

### [create\_and\_start\_with\_poly\_data2 ¶](#create_and_start_with_poly_data2) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L393)

```
create_and_start_with_poly_data2 :: proc(
	arg1:         $T1, 
	arg2:         $T2, 
	fn:           proc($T1, $T2), 
	init_context: runtime.Maybe($T=Context) = nil, 
	priority:     Thread_Priority = Thread_Priority.Normal, 
	self_cleanup: bool = false, 
) -> (t: ^Thread) {…}
```

 

Run a procedure with two polymorphic parameters on a different thread.

This procedure runs the given procedure on another thread. The context
specified by `init_context` will be used as the context in which `fn` is going
to execute. The thread will have priority specified by the `priority` parameter.

If `self_cleanup` is specified, after the thread finishes the execution of the
`fn` procedure, the resources associated with the thread are going to be
automatically freed.

**Do not** dereference the `^Thread` pointer, if this flag is specified.
That includes calling `join`, which needs to dereference ^Thread`.

**IMPORTANT**: If `init_context` is specified and the default temporary allocator
is used, the thread procedure needs to call `runtime.default_temp_allocator_destroy()`
in order to free the resources associated with the temporary allocations.

### [create\_and\_start\_with\_poly\_data3 ¶](#create_and_start_with_poly_data3) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L444)

```
create_and_start_with_poly_data3 :: proc(
	arg1:         $T1, 
	arg2:         $T2, 
	arg3:         $T3, 
	fn:           proc(arg1: $T1, arg2: $T2, arg3: $T3), 
	init_context: runtime.Maybe($T=Context) = nil, 
	priority:     Thread_Priority = Thread_Priority.Normal, 
	self_cleanup: bool = false, 
) -> (t: ^Thread) {…}
```

 

Run a procedure with three polymorphic parameters on a different thread.

This procedure runs the given procedure on another thread. The context
specified by `init_context` will be used as the context in which `fn` is going
to execute. The thread will have priority specified by the `priority` parameter.

If `self_cleanup` is specified, after the thread finishes the execution of the
`fn` procedure, the resources associated with the thread are going to be
automatically freed.

**Do not** dereference the `^Thread` pointer, if this flag is specified.
That includes calling `join`, which needs to dereference ^Thread`.

**IMPORTANT**: If `init_context` is specified and the default temporary allocator
is used, the thread procedure needs to call `runtime.default_temp_allocator_destroy()`
in order to free the resources associated with the temporary allocations.

### [create\_and\_start\_with\_poly\_data4 ¶](#create_and_start_with_poly_data4) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L497)

```
create_and_start_with_poly_data4 :: proc(
	arg1:         $T1, 
	arg2:         $T2, 
	arg3:         $T3, 
	arg4:         $T4, 
	fn:           proc(arg1: $T1, arg2: $T2, arg3: $T3, arg4: $T4), 
	init_context: runtime.Maybe($T=Context) = nil, 
	priority:     Thread_Priority = Thread_Priority.Normal, 
	self_cleanup: bool = false, 
) -> (t: ^Thread) {…}
```

 

Run a procedure with four polymorphic parameters on a different thread.

This procedure runs the given procedure on another thread. The context
specified by `init_context` will be used as the context in which `fn` is going
to execute. The thread will have priority specified by the `priority` parameter.

If `self_cleanup` is specified, after the thread finishes the execution of the
`fn` procedure, the resources associated with the thread are going to be
automatically freed.

**Do not** dereference the `^Thread` pointer, if this flag is specified.
That includes calling `join`, which needs to dereference ^Thread`.

**IMPORTANT**: If `init_context` is specified and the default temporary allocator
is used, the thread procedure needs to call `runtime.default_temp_allocator_destroy()`
in order to free the resources associated with the temporary allocations.

### [destroy ¶](#destroy) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L116)

```
destroy :: proc(thread: ^Thread) {…}
```

 

Wait for the thread to finish and free all data associated with it.

### [is\_done ¶](#is_done) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L130)

```
is_done :: proc(thread: ^Thread) -> bool {…}
```

 

Check if the thread has finished work.

### [join ¶](#join) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L137)

```
join :: proc(thread: ^Thread) {…}
```

 

Wait for the thread to finish work.

### [join\_multiple ¶](#join_multiple) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L144)

```
join_multiple :: proc(.. threads: ..^Thread) {…}
```

 

Wait for all threads to finish work.

### [pool\_add\_task ¶](#pool_add_task) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L190)

```
pool_add_task :: proc(pool: ^Pool, allocator: runtime.Allocator, procedure: Task_Proc, data: rawptr, user_index: int = 0) {…}
```

 

Add a task to the thread pool.

Tasks can be added from any thread, not just the thread that created
the thread pool. You can even add tasks from inside other tasks.

Each task also needs an allocator which it either owns, or which is thread
safe.

### [pool\_destroy ¶](#pool_destroy) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L126)

```
pool_destroy :: proc(pool: ^Pool) {…}
```

### [pool\_do\_work ¶](#pool_do_work) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L361)

```
pool_do_work :: proc(pool: ^Pool, task: Task) {…}
```

 

Mostly for internal use.

### [pool\_finish ¶](#pool_finish) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L377)

```
pool_finish :: proc(pool: ^Pool) {…}
```

 

Process the rest of the tasks, also use this thread for processing, then join
all the pool threads.

### [pool\_init ¶](#pool_init) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L94)

```
pool_init :: proc(
	pool:         ^Pool, 
	allocator:    runtime.Allocator, 
	thread_count: int, 
	init_proc:    Thread_Init_Proc = nil, 
	init_data:    rawptr = nil, 
	fini_proc:    Thread_Init_Proc = nil, 
	fini_data:    rawptr = nil, 
) {…}
```

 

Once initialized, the pool's memory address is not allowed to change until
it is destroyed.

The thread pool requires an allocator which it either owns, or which is thread safe.

### [pool\_is\_empty ¶](#pool_is_empty) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L329)

```
pool_is_empty :: proc(pool: ^Pool) -> bool {…}
```

 

If tasks are only being added from one thread, and this procedure is being
called from that same thread, it will reliably tell if the thread pool is
empty or not. Empty in this case means there are no tasks waiting, being
processed, or \_done\_.

### [pool\_join ¶](#pool_join) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L148)

```
pool_join :: proc(pool: ^Pool) {…}
```

 

Finish tasks that have already started processing, then shut down all pool
threads. Might leave over waiting tasks, any memory allocated for the
user data of those tasks will not be freed.

### [pool\_num\_done ¶](#pool_num_done) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L321)

```
pool_num_done :: proc(pool: ^Pool) -> int {…}
```

 

Number of tasks which are done processing. Only informational, mostly for
debugging. Don't rely on this value being consistent with other num\_\*
values.

### [pool\_num\_in\_processing ¶](#pool_num_in_processing) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L306)

```
pool_num_in_processing :: proc(pool: ^Pool) -> int {…}
```

 

Number of tasks currently being processed. Only informational, mostly for
debugging. Don't rely on this value being consistent with other num\_\*
values.

### [pool\_num\_outstanding ¶](#pool_num_outstanding) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L314)

```
pool_num_outstanding :: proc(pool: ^Pool) -> int {…}
```

 

Outstanding tasks are all tasks that are not done, that is, tasks that are
waiting, as well as tasks that are currently being processed. Only
informational, mostly for debugging. Don't rely on this value being
consistent with other num\_\* values.

### [pool\_num\_waiting ¶](#pool_num_waiting) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L299)

```
pool_num_waiting :: proc(pool: ^Pool) -> int {…}
```

 

Number of tasks waiting to be processed. Only informational, mostly for
debugging. Don't rely on this value being consistent with other num\_\*
values.

### [pool\_pop\_done ¶](#pool_pop_done) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L348)

```
pool_pop_done :: proc(pool: ^Pool) -> (task: Task, got_task: bool) {…}
```

 

Use this to take out finished tasks.

### [pool\_pop\_waiting ¶](#pool_pop_waiting) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L334)

```
pool_pop_waiting :: proc(pool: ^Pool) -> (task: Task, got_task: bool) {…}
```

 

Mostly for internal use.

### [pool\_shutdown ¶](#pool_shutdown) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L279)

```
pool_shutdown :: proc(pool: ^Pool, exit_code: int = 1) {…}
```

 

Force the pool to stop all of its threads and put it into a state where
it will no longer run any more tasks.

The pool must still be destroyed after this.

### [pool\_start ¶](#pool_start) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L139)

```
pool_start :: proc(pool: ^Pool) {…}
```

### [pool\_stop\_all\_tasks ¶](#pool_stop_all_tasks) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L247)

```
pool_stop_all_tasks :: proc(pool: ^Pool, exit_code: int = 1) {…}
```

 

Forcibly stop all running tasks.

The same notes from `pool_stop_task` apply here.

### [pool\_stop\_task ¶](#pool_stop_task) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin#L213)

```
pool_stop_task :: proc(pool: ^Pool, user_index: int, exit_code: int = 1) -> bool {…}
```

 

Forcibly stop a running task by its user index.

This will terminate the underlying thread. Ideally, you should use some
means of communication to stop a task, as thread termination may leave
resources unclaimed.

The thread will be restarted to accept new tasks.

Returns true if the task was found and terminated.

### [run ¶](#run) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L173)

```
run :: proc(fn: proc(), init_context: runtime.Maybe($T=Context) = nil, priority: Thread_Priority = Thread_Priority.Normal) {…}
```

 

Run a procedure on a different thread.

This procedure runs the given procedure on another thread. The context
specified by `init_context` will be used as the context in which `fn` is going
to execute. The thread will have priority specified by the `priority` parameter.

**IMPORTANT**: If `init_context` is specified and the default temporary allocator
is used, the thread procedure needs to call `runtime.default_temp_allocator_destroy()`
in order to free the resources associated with the temporary allocations.

### [run\_with\_data ¶](#run_with_data) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L188)

```
run_with_data :: proc(data: rawptr, fn: proc(data: rawptr), init_context: runtime.Maybe($T=Context) = nil, priority: Thread_Priority = Thread_Priority.Normal) {…}
```

 

Run a procedure with one pointer parameter on a different thread.

This procedure runs the given procedure on another thread. The context
specified by `init_context` will be used as the context in which `fn` is going
to execute. The thread will have priority specified by the `priority` parameter.

**IMPORTANT**: If `init_context` is specified and the default temporary allocator
is used, the thread procedure needs to call `runtime.default_temp_allocator_destroy()`
in order to free the resources associated with the temporary allocations.

### [run\_with\_poly\_data ¶](#run_with_poly_data) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L203)

```
run_with_poly_data :: proc(data: $T, fn: proc(data: $T), init_context: runtime.Maybe($T=Context) = nil, priority: Thread_Priority = Thread_Priority.Normal) {…}
```

 

Run a procedure with one polymorphic parameter on a different thread.

This procedure runs the given procedure on another thread. The context
specified by `init_context` will be used as the context in which `fn` is going
to execute. The thread will have priority specified by the `priority` parameter.

**IMPORTANT**: If `init_context` is specified and the default temporary allocator
is used, the thread procedure needs to call `runtime.default_temp_allocator_destroy()`
in order to free the resources associated with the temporary allocations.

### [run\_with\_poly\_data2 ¶](#run_with_poly_data2) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L219)

```
run_with_poly_data2 :: proc(arg1: $T1, arg2: $T2, fn: proc($T1, $T2), init_context: runtime.Maybe($T=Context) = nil, priority: Thread_Priority = Thread_Priority.Normal) {…}
```

 

Run a procedure with two polymorphic parameters on a different thread.

This procedure runs the given procedure on another thread. The context
specified by `init_context` will be used as the context in which `fn` is going
to execute. The thread will have priority specified by the `priority` parameter.

**IMPORTANT**: If `init_context` is specified and the default temporary allocator
is used, the thread procedure needs to call `runtime.default_temp_allocator_destroy()`
in order to free the resources associated with the temporary allocations.

### [run\_with\_poly\_data3 ¶](#run_with_poly_data3) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L235)

```
run_with_poly_data3 :: proc(
	arg1:         $T1, 
	arg2:         $T2, 
	arg3:         $T3, 
	fn:           proc(arg1: $T1, arg2: $T2, arg3: $T3), 
	init_context: runtime.Maybe($T=Context) = nil, 
	priority:     Thread_Priority = Thread_Priority.Normal, 
) {…}
```

 

Run a procedure with three polymorphic parameters on a different thread.

This procedure runs the given procedure on another thread. The context
specified by `init_context` will be used as the context in which `fn` is going
to execute. The thread will have priority specified by the `priority` parameter.

**IMPORTANT**: If `init_context` is specified and the default temporary allocator
is used, the thread procedure needs to call `runtime.default_temp_allocator_destroy()`
in order to free the resources associated with the temporary allocations.

### [run\_with\_poly\_data4 ¶](#run_with_poly_data4) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L251)

```
run_with_poly_data4 :: proc(
	arg1:         $T1, 
	arg2:         $T2, 
	arg3:         $T3, 
	arg4:         $T4, 
	fn:           proc(arg1: $T1, arg2: $T2, arg3: $T3, arg4: $T4), 
	init_context: runtime.Maybe($T=Context) = nil, 
	priority:     Thread_Priority = Thread_Priority.Normal, 
) {…}
```

 

Run a procedure with four polymorphic parameters on a different thread.

This procedure runs the given procedure on another thread. The context
specified by `init_context` will be used as the context in which `fn` is going
to execute. The thread will have priority specified by the `priority` parameter.

**IMPORTANT**: If `init_context` is specified and the default temporary allocator
is used, the thread procedure needs to call `runtime.default_temp_allocator_destroy()`
in order to free the resources associated with the temporary allocations.

### [start ¶](#start) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L123)

```
start :: proc(thread: ^Thread) {…}
```

 

Start a suspended thread.

### [terminate ¶](#terminate) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L151)

```
terminate :: proc(thread: ^Thread, exit_code: int) {…}
```

 

Forcibly terminate a running thread.

### [yield ¶](#yield) [*Source*](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin#L158)

```
yield :: proc() {…}
```

 

Yield the execution of the current thread to another OS thread or process.

## Procedure Groups

This section is empty.

## Source Files

* [thread.odin](https://github.com/odin-lang/Odin/tree/master/core/thread/thread.odin)
* [thread\_other.odin](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_other.odin)
* [thread\_pool.odin](https://github.com/odin-lang/Odin/tree/master/core/thread/thread_pool.odin)

## Generation Information

Generated with `odin version dev-2026-04 (vendor "odin") Windows_amd64 @ 2026-04-19 21:26:51.393284600 +0000 UTC`

* [Overview](#pkg-overview)* [Types](#pkg-Types)
    + [Pool](#Pool)
    + [Pool\_Thread\_Data](#Pool_Thread_Data)
    + [Task](#Task)
    + [Task\_Proc](#Task_Proc)
    + [Thread](#Thread)
    + [Thread\_Fini\_Proc](#Thread_Fini_Proc)
    + [Thread\_Init\_Proc](#Thread_Init_Proc)
    + [Thread\_Os\_Specific](#Thread_Os_Specific)
    + [Thread\_Priority](#Thread_Priority)
    + [Thread\_Proc](#Thread_Proc)
    + [Thread\_State](#Thread_State)
  * [Constants](#pkg-Constants)
    + [IS\_SUPPORTED](#IS_SUPPORTED)
    + [MAX\_USER\_ARGUMENTS](#MAX_USER_ARGUMENTS)
  * [Procedures](#pkg-Procedures)
    + [create](#create)
    + [create\_and\_start](#create_and_start)
    + [create\_and\_start\_with\_data](#create_and_start_with_data)
    + [create\_and\_start\_with\_poly\_data](#create_and_start_with_poly_data)
    + [create\_and\_start\_with\_poly\_data2](#create_and_start_with_poly_data2)
    + [create\_and\_start\_with\_poly\_data3](#create_and_start_with_poly_data3)
    + [create\_and\_start\_with\_poly\_data4](#create_and_start_with_poly_data4)
    + [destroy](#destroy)
    + [is\_done](#is_done)
    + [join](#join)
    + [join\_multiple](#join_multiple)
    + [pool\_add\_task](#pool_add_task)
    + [pool\_destroy](#pool_destroy)
    + [pool\_do\_work](#pool_do_work)
    + [pool\_finish](#pool_finish)
    + [pool\_init](#pool_init)
    + [pool\_is\_empty](#pool_is_empty)
    + [pool\_join](#pool_join)
    + [pool\_num\_done](#pool_num_done)
    + [pool\_num\_in\_processing](#pool_num_in_processing)
    + [pool\_num\_outstanding](#pool_num_outstanding)
    + [pool\_num\_waiting](#pool_num_waiting)
    + [pool\_pop\_done](#pool_pop_done)
    + [pool\_pop\_waiting](#pool_pop_waiting)
    + [pool\_shutdown](#pool_shutdown)
    + [pool\_start](#pool_start)
    + [pool\_stop\_all\_tasks](#pool_stop_all_tasks)
    + [pool\_stop\_task](#pool_stop_task)
    + [run](#run)
    + [run\_with\_data](#run_with_data)
    + [run\_with\_poly\_data](#run_with_poly_data)
    + [run\_with\_poly\_data2](#run_with_poly_data2)
    + [run\_with\_poly\_data3](#run_with_poly_data3)
    + [run\_with\_poly\_data4](#run_with_poly_data4)
    + [start](#start)
    + [terminate](#terminate)
    + [yield](#yield)
  * [Source Files](#pkg-source-files)



[![Odin](https://odin-lang.org/logo.svg)](https://odin-lang.org)

The Data-Oriented Language for Sane Software Development.

#### Resources

* [Docs](https://odin-lang.org/docs)
* [Packages](https://pkg.odin-lang.org/)
* [News](https://odin-lang.org/news)


#### Community

* [GitHub](https://github.com/odin-lang/Odin)
* [Forum](https://forum.odin-lang.org)
* [Discord](https://discord.com/invite/sVBPHEv)
* [Twitch](https://www.twitch.tv/ginger_bill)
* [YouTube](https://www.youtube.com/channel/UCUSck1dOH7VKmG4lRW7tZXg)
* [Showcase](https://odin-lang.org/showcase)


#### Contribute

* [Issues](https://github.com/odin-lang/Odin/issues)
* [Donate](https://www.patreon.com/gingerbill)

© 2016–2023 Ginger Bill